import { Inter } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = {
    className: "font-sans", // Replace with your actual font class if needed
};